# Фаза 0. Product Definition и MVP baseline

## Статус документа

Это рабочий черновик, который должен закрыть `Фазу 0` и дать переход к следующим фазам.

Документ основан на:

- старом Django-проекте в `reference/bboard`;
- старом Angular-клиенте в `reference/bbclient`;
- текущем решении делать новый проект как `SPA` на `SvelteKit + TypeScript` с отдельным Python API.

Ниже зафиксированы не только наблюдения, но и предлагаемые решения для `v1`.

## 0.1. Инвентаризация старого приложения

### Что реально есть в старой версии

Старое приложение уже покрывает базовый сценарий доски объявлений:

- регистрация пользователя;
- логин и профиль;
- активация по email;
- смена и сброс пароля;
- двухуровневые рубрики;
- список объявлений;
- карточка объявления;
- создание, редактирование и удаление объявления;
- основное изображение и дополнительные изображения;
- комментарии к объявлениям;
- простой JSON API;
- staff-функции через Django admin.

### Основные сущности старой версии

- `AdvUser`
- `Rubric` / `SuperRubric` / `SubRubric`
- `Bb` как объявление
- `AdditionalImage`
- `Comment`

### Экранный состав старой версии

- главная;
- список объявлений по рубрике;
- карточка объявления;
- регистрация;
- логин;
- профиль;
- создание объявления;
- редактирование объявления;
- удаление объявления;
- служебные страницы аккаунта;
- административный интерфейс.

### API-возможности старой версии

- список объявлений;
- карточка объявления;
- список комментариев;
- добавление комментария.

### Что в старом коде считать долгом, а не наследием

- `price` хранится как `float`, а не как денежный тип;
- автор объявления прокидывается через форму, а не назначается сервером;
- нет нормальной проверки владельца объявления в пользовательских страницах;
- автора комментария можно подделать через API;
- комментарии и права выглядят как учебная реализация;
- нет тестов;
- фронтенд и API не объединены единым контрактом.

### Вывод по инвентаризации

Старое приложение достаточно маленькое, чтобы не заниматься "бережной миграцией" по коду. Его надо использовать как источник:

- бизнес-сценариев;
- экранов;
- доменных сущностей;
- пограничных случаев.

Но не как источник архитектурных решений.

## 0.2. Роли и матрица прав

### Предлагаемые роли для новой системы

- `guest`
- `user`
- `staff`

### Матрица прав

| Действие                           | Guest | User | Staff |
| ---------------------------------- | ----- | ---- | ----- |
| Смотреть каталог                   | Да    | Да   | Да    |
| Смотреть карточку объявления       | Да    | Да   | Да    |
| Искать и фильтровать объявления    | Да    | Да   | Да    |
| Регистрироваться и логиниться      | Да    | Нет  | Нет   |
| Создавать объявление               | Нет   | Да   | Да    |
| Редактировать свое объявление      | Нет   | Да   | Да    |
| Удалять свое объявление            | Нет   | Да   | Да    |
| Редактировать чужое объявление     | Нет   | Нет  | Да    |
| Загружать изображения              | Нет   | Да   | Да    |
| Оставлять комментарий              | Нет   | Да   | Да    |
| Модерировать комментарии           | Нет   | Нет  | Да    |
| Управлять категориями              | Нет   | Нет  | Да    |
| Просматривать список пользователей | Нет   | Нет  | Да    |

### Предлагаемое правило доступа

- `guest` только читает публичный контент;
- `user` управляет только своими объявлениями и своими пользовательскими действиями;
- `staff` имеет отдельный служебный контур.

### Решение для `v1`

Для первой версии предлагаю не поддерживать гостевые комментарии.

Причина:

- это упрощает auth-модель;
- это снимает капчу и часть антиспам-слоя;
- это делает права и аудит понятнее;
- это лучше подходит под SPA и typed API.

## 0.3. Пользовательские сценарии

### Публичные сценарии

#### `PUB-01`

Гость открывает главную страницу и видит каталог объявлений.

#### `PUB-02`

Гость фильтрует объявления по категории.

#### `PUB-03`

Гость ищет объявления по тексту.

#### `PUB-04`

Гость открывает карточку объявления и смотрит описание, цену, изображения и комментарии.

#### `PUB-05`

Гость переходит на регистрацию или логин.

### Сценарии авторизованного пользователя

#### `USR-01`

Пользователь регистрируется.

#### `USR-02`

Пользователь входит в систему.

#### `USR-03`

Пользователь открывает свой профиль и видит свои объявления.

#### `USR-04`

Пользователь создает объявление.

#### `USR-05`

Пользователь загружает основное и дополнительные изображения объявления.

#### `USR-06`

Пользователь редактирует свое объявление.

#### `USR-07`

Пользователь удаляет свое объявление.

#### `USR-08`

Пользователь оставляет комментарий к объявлению.

#### `USR-09`

Пользователь меняет пароль.

#### `USR-10`

Пользователь восстанавливает доступ.

### Staff-сценарии

#### `STF-01`

Staff просматривает служебный список комментариев.

#### `STF-02`

Staff скрывает комментарий.

#### `STF-03`

Staff управляет категориями.

#### `STF-04`

Staff просматривает список пользователей.

#### `STF-05`

Staff скрывает или ограничивает объявление при необходимости.

### Приоритет сценариев

#### Обязательные для `v1`

- `PUB-01`
- `PUB-02`
- `PUB-03`
- `PUB-04`
- `PUB-05`
- `USR-01`
- `USR-02`
- `USR-03`
- `USR-04`
- `USR-05`
- `USR-06`
- `USR-07`
- `USR-08`
- `USR-09`
- `USR-10`
- `STF-01`
- `STF-02`
- `STF-03`

#### Желательные, но не обязательные для `v1`

- `STF-04`
- `STF-05`

### Первый vertical slice

Первым вертикальным срезом предлагаю делать не объявления, а auth-контур:

- регистрация;
- логин;
- logout;
- current user;
- базовый приватный экран.

Причина:

- это проверяет весь стек сразу;
- это рано фиксирует auth-модель;
- это создает базу для всех приватных сценариев дальше.

## 0.4. Предлагаемые продуктовые решения для `v1`

### 1. Гостевые комментарии

Решение:

- `Нет` в `v1`.

Почему:

- меньше антиспама;
- чище permission model;
- проще UI и API;
- лучше аудит.

### 2. Email-активация после регистрации

Решение:

- `Нет` в `v1`, но email остается обязательным полем.

Почему:

- это не критично для базовой ценности продукта;
- сильно увеличивает объем auth-flow и edge-cases;
- в `v1` лучше ограничиться регистрацией, логином и восстановлением доступа.

Что вместо этого:

- обычная регистрация;
- верификацию email можно вернуть во `v2`.

### 3. Staff backoffice

Решение:

- `Да`, но только минимальный.

Что входит:

- категории;
- комментарии;
- базовые действия по объявлениям.

Что не входит:

- попытка клонировать Django admin;
- универсальная CRUD-панель на все сущности.

### 4. Модерация объявлений

Решение:

- `Нет` на публикацию до одобрения;
- `Да` на возможность staff скрыть объявление постфактум.

Почему:

- premoderation сильно замедляет запуск и усложняет UX;
- post-moderation достаточно для `v1`.

### 5. Черновики объявлений

Решение:

- `Нет` в `v1`.

Почему:

- это отдельный lifecycle;
- требует дополнительных статусов, UI и логики;
- не является обязательной ценностью первой версии.

### 6. Несколько изображений на объявление

Решение:

- `Да` в `v1`.

Почему:

- это уже есть в старой версии;
- это важно для ценности объявления;
- это сразу задает нормальную файловую модель.

### 7. Soft delete

Решение:

- `Да` для объявлений в `v1`.

Что делаем:

- пользователь может удалить свое объявление;
- на уровне модели удаление объявления реализуется как `soft delete`;
- staff может скрывать нежелательный контент отдельно.

Почему:

- это безопаснее прямого удаления;
- это оставляет пространство для будущего восстановления или аудита;
- при этом в `v1` не требуется полноценный сложный recycle bin UX.

### 8. Модерация комментариев

Решение:

- `Да`, но постфактум.

Что это значит:

- комментарий появляется сразу;
- staff может скрыть его позже.

### 9. Иерархия категорий

Решение:

- модель делаем через `parent_id`;
- в `v1` поддерживаем максимум два уровня отображения.

Почему:

- модель будет гибкой;
- UI и бизнес-правила останутся простыми.

### 10. Аутентификация для SPA

Решение:

- cookie/session auth;
- `HttpOnly` cookie;
- API под тем же origin, без лишнего CORS-зоопарка.

Почему:

- для браузерного SPA это проще и надежнее, чем `JWT` в `localStorage`.

## 0.5. Границы MVP

### Что входит в `MVP`

- регистрация;
- логин;
- logout;
- current user;
- восстановление доступа;
- просмотр каталога;
- фильтрация и поиск;
- карточка объявления;
- создание объявления;
- редактирование своего объявления;
- удаление своего объявления;
- загрузка нескольких изображений;
- комментарии только для авторизованных пользователей;
- минимальный staff backoffice для категорий и комментариев.

### Что явно не входит в `MVP`

- email-активация;
- гостевые комментарии;
- черновики объявлений;
- премодерация объявлений;
- полнофункциональная админка;
- избранное;
- чат между пользователями;
- уведомления в реальном времени;
- мобильное приложение;
- публичный внешний API для третьих сторон.

### Что входит в `post-MVP`

- email verification;
- расширенный staff контур;
- аудит действий staff;
- soft delete и восстановление;
- избранное;
- richer profile;
- заметная продуктовая аналитика;
- rate limit и антиспам-улучшения;
- более сложная модерация контента.

### Одноабзацное описание `v1`

`v1` это SPA-доска объявлений, где гость может смотреть каталог и карточки, а зарегистрированный пользователь может создать объявление с несколькими изображениями, управлять своими объявлениями и оставлять комментарии. Staff может управлять категориями и скрывать нежелательные комментарии.

## 0.6. Acceptance checklist для `v1`

Первая версия считается готовой, если:

- пользователь может зарегистрироваться и войти;
- пользователь может восстановить доступ;
- пользователь может открыть свой профиль;
- пользователь может создать объявление;
- пользователь может загрузить к объявлению несколько изображений;
- пользователь может редактировать только свои объявления;
- пользователь может удалить только свои объявления;
- гость может просматривать каталог, фильтровать и искать объявления;
- гость может открыть карточку объявления;
- авторизованный пользователь может оставить комментарий;
- staff может управлять категориями;
- staff может скрыть комментарий;
- вся основная функциональность проходит через новый SPA и новый API;
- основные пользовательские сценарии покрыты тестами хотя бы на smoke/integration уровне.

## 0.7. Стартовый backlog

### Iteration 1. Foundations

- создать структуру `apps/api`, `apps/web`, `packages/api-types`, `infra`;
- поднять `docker-compose` с `postgres`, `mailpit`, `minio`;
- настроить `Litestar` foundation;
- настроить `SvelteKit` foundation;
- подключить OpenAPI и генерацию типов;
- подключить `TanStack Query`, `Vitest`, `Playwright`, `Storybook`.

### Iteration 2. Первый vertical slice

- backend:
  - регистрация;
  - логин;
  - logout;
  - current user;
  - session auth;
- frontend:
  - страницы регистрации и логина;
  - базовый shell приложения;
  - guard приватного маршрута;
  - `me` query;
  - состояние сессии в UI.

### Iteration 3. Публичный read-only контур

- backend:
  - категории;
  - список объявлений;
  - фильтры;
  - поиск;
  - карточка объявления;
- frontend:
  - главная;
  - каталог;
  - карточка;
  - состояния загрузки и пустых результатов.

### Iteration 4. Личный кабинет и объявления

- backend:
  - create/update/delete ad;
  - uploads;
  - owner checks;
- frontend:
  - кабинет;
  - форма создания;
  - форма редактирования;
  - управление изображениями.

### Iteration 5. Комментарии и staff

- backend:
  - create/list comments;
  - hide comment;
  - categories management;
- frontend:
  - UI комментариев;
  - минимальный staff контур.

## Что нужно от вас при чтении документа

Проверьте в первую очередь не формулировки, а решения:

- точно ли убираем гостевые комментарии из `v1`;
- точно ли убираем email-активацию из `v1`;
- достаточно ли минимального staff backoffice;
- нужна ли модерация объявлений в `v1`;
- согласны ли вы не делать черновики сразу;
- устраивает ли состав `MVP`.
